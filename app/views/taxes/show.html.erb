<div class="flex items-center justify-between mb-2">
    <h1 class="pl-2 pt-6 pb-4 text-2xl font-semibold">Guia <%= @tax.id %> - <%= @tax.tax_type %></h1>
    <%= link_to "Voltar",  taxes_path, class: "btn btn-primary"  %>
</div>

Valor: <%= number_to_currency(@tax.amount, :unit => 'R$', :separator => ',', :delimiter => ".") %><br>
Data de vencimento: <%= @tax.due_date %><br>

<div class="flex flex-row items-center pt-4 gap-4">
  <%= link_to "Editar", edit_tax_path(@tax),  class: "btn btn-primary" %>
</div>
